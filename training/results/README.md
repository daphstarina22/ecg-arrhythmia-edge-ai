# Phase 2 Results

This directory is reserved for exported audit tables, confusion matrices,
feature-importance tables, and experiment comparison reports. It is empty by
design until the raw Kaggle feature table is supplied and the controlled
experiments are approved.

Do not place replacement ONNX files here. Experimental models must remain
separate from the deployed `models/` directory.